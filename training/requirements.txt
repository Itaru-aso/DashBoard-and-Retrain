# training/ 実行用の依存パッケージ一覧（Python 3.10・CUDA 12.1）。
# 実際に動作確認済みの学習実行環境の pip freeze から、training/ が import するパッケージのみ抽出。
# torch/torchvision は PyTorch 公式 index（cu121）が必要（Dockerfile で --extra-index-url 指定）。
--extra-index-url https://download.pytorch.org/whl/cu121
torch==2.3.1+cu121
torchvision==0.18.1+cu121
numpy==1.26.4
pandas==2.2.2
pillow==10.3.0
opencv-python==4.9.0.80
mlflow==2.12.2
omegaconf==2.3.0
onnx==1.16.0
onnxruntime-gpu==1.18.0
scikit-learn==1.2.2
seaborn==0.13.2
tifffile==2024.5.22
tqdm==4.56.0
